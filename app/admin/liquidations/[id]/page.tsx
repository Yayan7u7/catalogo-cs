import PageHeader from "@/components/ui/page-header";

export default async function LiquidationDetailPage() {
  return (
    <PageHeader
      title="Detalle de Liquidacion"
      description="Este modulo estara disponible cuando el backend tenga el endpoint de liquidaciones."
    />
  );
}